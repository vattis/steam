spring:
  profiles:
    group:
      "local": "common, h2, embedded_redis"
      "dev": "common, mysql, embedded_redis"
      "prod": "common, mysql"
      "dev-load-test": "common, mysql, load"
---

spring:
  application:
    name: steam
  config:
    activate:
      on-profile: common
  jpa:
    defer-datasource-initialization: true
    generate-ddl: true
  sql:
    init:
      mode: always
  web:
    resources:
      static-locations: classpath:/static/
  jwt:
    secret: YW5kcmV3MDI3MzkyNzg2Mjk0NzQ2Mjk1NjUyOTM3NjQ5NQ==
  mail:
    mail:
      host: smtp.gmail.com
      port: 587
      username: superhsk00@gmail.com
      password: kvaelytnqmpofrfy
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
              required: true
            connectiontimeout: 5000
            timeout: 5000
            writetimeout: 5000
        auth-code-expiration-millis: 1800000


---

spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:tcp://localhost/~/steam
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

---

spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/steam?serverTimezone=Asia/Seoul
    username: root
    password: nexonhsk00
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true
        format_sql: true

---

spring:
  config:
    activate:
      on-profile: dev
management:
  endpoints:
    web:
      exposure:
        include: refresh,health,beans,metrics,info,threaddump,headdump,prometheus
  endpoint:
    health:
      show-details: always
    metrics:
      distribution:
        percentiles:
          http.server.requests: 0.5,0.95,0.99
        percentiles-histogram:
          http.server.requests: true

---
spring:
  config:
    activate:
      on-profile: load
logging:
  level:
    org:
      springframework:
        web:
          socket: DEBUG
          messaging: DEBUG
server:
  tomcat:
    threads:
      max: 400
    accept-count: 10000
    max-connections: 20000


---
spring:
  config:
    activate:
      on-profile: embedded_redis
  data:
    redis:
      port: 6379
      host: 127.0.0.1
      ssl:
        enabled: true
      database: 0
